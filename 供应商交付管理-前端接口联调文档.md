# 供应商交付管理 — 前端接口联调文档

> 更新时间：2025-02-08
> 后端已编译通过，所有接口可联调

---

## 一、菜单结构

### Admin 后台

```
📦 供应商交付管理
├── 📋 请购单管理                → /admin/purchase/request
├── 📋 采购订单管理              → /admin/purchase/order
├── 📋 交付确认记录              → /admin/delivery/confirmation
└── 📊 交付时效分析              → /admin/delivery/timeliness
```

### 供应商端

```
📦 我的交付
├── 📋 我的采购订单              → /vendor/delivery/myPurchaseOrders
├── 📋 我的交付记录              → /vendor/delivery/myConfirmations
└── 📊 我的交付统计              → /vendor/delivery/myTimeliness + /myStats
```

---

## 二、通用说明

### 响应格式

```json
{
  "code": 200,
  "msg": "加载成功",
  "data": { ... }
}
```

### 分页参数（所有列表接口通用）

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| p | int | 否 | 页码，默认 1 |
| l | int | 否 | 每页数量，默认 10 |

### 分页响应结构（PageTO）

```json
{
  "total": 100,
  "list": [ ... ]
}
```

### 时间参数格式

所有 `beginTime` / `endTime` 参数格式为 `yyyy-MM-dd`，例如 `2025-02-01`

---

## 三、Admin 后台接口

---

### 3.1 请购单管理

#### 3.1.1 请购单列表（含采购和交付汇总）

```
GET /admin/purchase/request/list
```

**请求参数：**

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| requestCode | String | 否 | 请购单号（精确匹配） |
| beginTime | String | 否 | 开始时间 yyyy-MM-dd（按请购日期筛选） |
| endTime | String | 否 | 结束时间 yyyy-MM-dd |
| p | int | 否 | 页码 |
| l | int | 否 | 每页数量 |

**响应字段（PurchaseRequestSummaryDTO）：**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Long | 主键ID |
| requestCode | String | 请购单号 |
| requestDate | String | 请购日期 (yyyy-MM-dd) |
| invCode | String | 存货编码 |
| quantity | BigDecimal | 请购数量 |
| rowNo | Integer | 行号 |
| suggestVendor | String | 建议供应商 |
| soCode | String | 销售订单号 |
| rowBarcode | String | 请购单行条码 |
| remark | String | 备注 |
| requireDate | String | 需求日期 (yyyy-MM-dd) |
| isPoPlaced | Integer | 是否已下采购单 (0=否, 1=是) |
| poCount | Integer | 关联采购订单数 |
| poTotalQty | BigDecimal | 采购总数量 |
| deliveredQty | BigDecimal | 已交付总数量 |
| latestDeliveryDate | String | 最近交付日期 (yyyy-MM-dd) |
| maxOverdueDays | Integer | 最大超期天数 |

---

#### 3.1.2 ERP同步请购单

```
POST /admin/purchase/request/erp/create
```

> 免鉴权（@PassToken）

**请求体：** `List<PurchaseRequisitionVO>` — ERP 推送数据，存在则修改否则新增

---

### 3.2 采购订单管理

#### 3.2.1 采购订单列表

```
GET /admin/purchase/order/list
```

**请求参数：**

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| poCode | String | 否 | 采购订单号（精确匹配） |
| vendorCode | String | 否 | 供应商编码（精确匹配） |
| appCode | String | 否 | 请购单号（精确匹配） |
| beginTime | String | 否 | 开始时间 yyyy-MM-dd（按制单时间筛选） |
| endTime | String | 否 | 结束时间 yyyy-MM-dd |
| p | int | 否 | 页码 |
| l | int | 否 | 每页数量 |

**响应字段（TPurchaseOrderEntity）：** 采购订单全字段，关键字段如下

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Long | 主键ID |
| poCode | String | 采购订单号 |
| poDate | String | 订单日期 |
| vendorCode | String | 供应商编码 |
| appCode | String | 关联请购单号 |
| invCode | String | 存货编码 |
| quantity | BigDecimal | 数量 |
| planArriveDate | String | 计划到货日期 |
| makeTime | String | 制单时间 |
| rowNo | Integer | 行号 |
| rowBarcode | String | 行条码 |

---

#### 3.2.2 采购订单交付汇总列表

```
GET /admin/purchase/order/delivery/summary/list
```

**请求参数：**

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| poCode | String | 否 | 采购订单号（精确匹配） |
| vendorCode | String | 否 | 供应商编码（精确匹配） |
| appCode | String | 否 | 请购单号（精确匹配） |
| beginTime | String | 否 | 开始时间 yyyy-MM-dd（按制单时间筛选） |
| endTime | String | 否 | 结束时间 yyyy-MM-dd |
| p | int | 否 | 页码 |
| l | int | 否 | 每页数量 |

**响应字段（PurchaseOrderDeliveryDTO）：**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Long | 主键ID |
| poCode | String | 采购订单号 |
| rowNo | Integer | 行号 |
| invCode | String | 存货编码 |
| quantity | BigDecimal | 采购数量 |
| planArriveDate | String | 计划到货日期 (yyyy-MM-dd) |
| vendorCode | String | 供应商编码 |
| appCode | String | 关联请购单号 |
| rowBarcode | String | 采购订单行条码 |
| cmaketime | String | 制单时间 (yyyy-MM-dd HH:mm:ss) |
| rowRemark | String | 行备注 |
| deliveredQty | BigDecimal | 已交付数量 |
| remainingQty | BigDecimal | 剩余待交付数量 |
| deliveryStatus | String | 交付状态：未交付 / 部分交付 / 已完成 |
| overdueDays | Integer | 超期天数 |

---

#### 3.2.3 ERP同步采购订单

```
POST /admin/purchase/order/erp/create
```

> 免鉴权（@PassToken）

**请求体：** `List<PurchaseOrderVO>` — ERP 推送数据，存在则修改否则新增

---

### 3.3 交付确认记录

#### 3.3.1 交付确认列表

```
GET /admin/delivery/confirmation/list
```

**请求参数：**

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| poCode | String | 否 | 采购订单号（精确匹配） |
| vendorCode | String | 否 | 供应商编码（精确匹配） |
| invCode | String | 否 | 存货编码（精确匹配） |
| beginTime | String | 否 | 开始时间 yyyy-MM-dd（按创建时间筛选） |
| endTime | String | 否 | 结束时间 yyyy-MM-dd |
| p | int | 否 | 页码 |
| l | int | 否 | 每页数量 |

**响应字段（TDeliveryConfirmationEntity）：**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Long | 主键ID |
| poId | Long | 采购订单行ID |
| vendorCode | String | 供应商编码 |
| invCode | String | 存货编码 |
| poCode | String | 采购订单号 |
| poRowBarcode | String | 采购订单行条码 |
| poRowNo | Integer | 采购订单行号 |
| deliveryQty | BigDecimal | 本次交付数量 |
| actualDeliveryDate | String | 实际交付日期 (yyyy-MM-dd) |
| overdueDays | Integer | 超期天数（正=超期, 负=提前, 0=准时） |
| isOverdue | Boolean | 是否超期 |
| remark | String | 备注 |
| createBy | String | 创建人 |
| createTime | String | 创建时间 (yyyy-MM-dd HH:mm:ss) |

---

#### 3.3.2 根据采购订单行查询交付确认记录

```
GET /admin/delivery/confirmation/byPoId/{poId}
```

**路径参数：**

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| poId | Long | 是 | 采购订单行ID |

**响应：** `List<TDeliveryConfirmationEntity>` 字段同上

---

### 3.4 交付时效分析

#### 3.4.1 交付时效统计列表（订单行维度）

```
GET /admin/delivery/timeliness/list
```

**请求参数：**

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| poCode | String | 否 | 采购订单号（精确匹配） |
| vendorCode | String | 否 | 供应商编码（精确匹配） |
| requestCode | String | 否 | 请购单号（精确匹配） |
| isOverdue | Boolean | 否 | 是否超期 |
| isCompleted | Boolean | 否 | 是否交付完成 |
| beginTime | String | 否 | 开始时间 yyyy-MM-dd（按创建时间筛选） |
| endTime | String | 否 | 结束时间 yyyy-MM-dd |
| p | int | 否 | 页码 |
| l | int | 否 | 每页数量 |

**响应字段（TDeliveryTimelinessEntity）：**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Long | 主键ID |
| requestCode | String | 请购单号 |
| requestRowBarcode | String | 请购单行条码 |
| poCode | String | 采购订单号 |
| poRowBarcode | String | 采购订单行条码 |
| poId | Long | 采购订单行ID |
| vendorCode | String | 供应商编码 |
| invCode | String | 存货编码 |
| requestDate | String | 请购日期 (yyyy-MM-dd) |
| requestRequireDate | String | 请购需求日期 (yyyy-MM-dd) |
| poDate | String | 采购下单日期 (yyyy-MM-dd) |
| planArriveDate | String | 计划到货日期 (yyyy-MM-dd) |
| firstDeliveryDate | String | 首次交付日期 (yyyy-MM-dd) |
| lastDeliveryDate | String | 最后交付日期 (yyyy-MM-dd) |
| requestedQty | BigDecimal | 请购数量 |
| orderedQty | BigDecimal | 采购订单数量 |
| totalDeliveredQty | BigDecimal | 累计交付数量 |
| deliveryCount | Integer | 交付批次数 |
| requestToPoDays | Integer | 请购→下单天数 |
| poToFirstDeliveryDays | Integer | 下单→首次交付天数 |
| requestToDeliveryDays | Integer | 请购→最终交付天数（全流程） |
| planVsActualDays | Integer | 计划vs实际偏差天数（正=超期） |
| isOverdue | Boolean | 是否超期 |
| isCompleted | Boolean | 是否交付完成 |
| completionRate | BigDecimal | 交付完成率 (%) |
| createTime | String | 创建时间 |
| updateTime | String | 更新时间 |

---

#### 3.4.2 请购单交付汇总列表（请购单维度）

```
GET /admin/delivery/timeliness/request/summary/list
```

**请求参数：**

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| requestCode | String | 否 | 请购单号（精确匹配） |
| beginTime | String | 否 | 开始时间 yyyy-MM-dd（按请购日期筛选） |
| endTime | String | 否 | 结束时间 yyyy-MM-dd |
| p | int | 否 | 页码 |
| l | int | 否 | 每页数量 |

**响应字段：** 同 3.1.1 PurchaseRequestSummaryDTO

---

#### 3.4.3 供应商交付统计概览（供应商维度）

```
GET /admin/delivery/timeliness/vendor/stats
```

**请求参数：**

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| vendorCode | String | **是** | 供应商编码 |

**响应字段（VendorDeliveryStatsVO）：**

| 字段 | 类型 | 说明 |
|------|------|------|
| vendorCode | String | 供应商编码 |
| totalOrderedQty | BigDecimal | 累计订购数量 |
| totalDeliveredQty | BigDecimal | 累计交付数量 |
| completionRate | BigDecimal | 交付完成率 (%) |
| overdueOrderLines | Long | 超期订单行数 |
| totalOrderLines | Long | 总订单行数 |

---

## 四、供应商端接口

> 所有供应商端接口自动从 Token 中获取当前供应商的 vendorCode，无需前端传入

---

### 4.1 提交交付确认

```
POST /vendor/delivery/submit
```

**请求体（DeliveryConfirmationDTO）：**

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| poId | Long | 是 | 采购订单行ID |
| deliveryQty | BigDecimal | 是 | 本次交付数量 |
| actualDeliveryDate | String | 是 | 实际交付日期 (yyyy-MM-dd) |
| remark | String | 否 | 备注 |

**响应：** `Boolean` true=成功

---

### 4.2 我的采购订单列表（含交付汇总）

```
GET /vendor/delivery/myPurchaseOrders
```

**请求参数：**

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| poCode | String | 否 | 采购订单号（精确匹配） |
| appCode | String | 否 | 请购单号（精确匹配） |
| beginTime | String | 否 | 开始时间 yyyy-MM-dd（按制单时间筛选） |
| endTime | String | 否 | 结束时间 yyyy-MM-dd |
| p | int | 否 | 页码 |
| l | int | 否 | 每页数量 |

**响应字段：** 同 3.2.2 PurchaseOrderDeliveryDTO

---

### 4.3 我的交付确认记录

```
GET /vendor/delivery/myConfirmations
```

**请求参数：**

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| poCode | String | 否 | 采购订单号（精确匹配） |
| beginTime | String | 否 | 开始时间 yyyy-MM-dd（按创建时间筛选） |
| endTime | String | 否 | 结束时间 yyyy-MM-dd |
| p | int | 否 | 页码 |
| l | int | 否 | 每页数量 |

**响应字段：** 同 3.3.1 TDeliveryConfirmationEntity

---

### 4.4 我的交付时效统计

```
GET /vendor/delivery/myTimeliness
```

**请求参数：**

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| poCode | String | 否 | 采购订单号（精确匹配） |
| isOverdue | Boolean | 否 | 是否超期 |
| isCompleted | Boolean | 否 | 是否交付完成 |
| beginTime | String | 否 | 开始时间 yyyy-MM-dd（按创建时间筛选） |
| endTime | String | 否 | 结束时间 yyyy-MM-dd |
| p | int | 否 | 页码 |
| l | int | 否 | 每页数量 |

**响应字段：** 同 3.4.1 TDeliveryTimelinessEntity

---

### 4.5 我的交付统计概览

```
GET /vendor/delivery/myStats
```

**请求参数：** 无（自动取当前供应商）

**响应字段：** 同 3.4.3 VendorDeliveryStatsVO

---

## 五、新增索引 SQL

```sql
-- 请购单：按请购日期筛选
ALTER TABLE `t_purchase_request` ADD INDEX `idx_request_date` (`request_date`) USING BTREE COMMENT '请购日期索引';

-- 采购订单：按请购单号搜索 + 按制单时间筛选
ALTER TABLE `t_purchase_order` ADD INDEX `idx_app_code` (`app_code`) USING BTREE COMMENT '关联请购单号索引';
ALTER TABLE `t_purchase_order` ADD INDEX `idx_make_time` (`make_time`) USING BTREE COMMENT '制单时间索引';

-- 交付确认：按创建时间筛选
ALTER TABLE `t_delivery_confirmation` ADD INDEX `idx_create_time` (`create_time`) USING BTREE COMMENT '创建时间索引';

-- 交付时效统计：按创建时间筛选
ALTER TABLE `t_delivery_timeliness` ADD INDEX `idx_create_time` (`create_time`) USING BTREE COMMENT '创建时间索引';
```

---

## 六、页面设计要点

### Admin 端

| 页面 | 筛选条件 | 关键操作 |
|------|---------|---------|
| 请购单管理 | 请购单号、时间范围 | 查看关联采购订单 |
| 采购订单管理 | 采购订单号、供应商编码、请购单号、时间范围 | Tab切换：基础列表 / 交付汇总；行操作：查看交付明细 |
| 交付确认记录 | 采购订单号、供应商编码、存货编码、时间范围 | 审计追溯 |
| 交付时效分析 | Tab切换：订单行维度 / 请购单维度 / 供应商维度 | 订单行：全部筛选条件；请购单：单号+时间；供应商：输入编码查统计卡片 |

### 供应商端

| 页面 | 筛选条件 | 关键操作 |
|------|---------|---------|
| 我的采购订单 | 采购订单号、请购单号、时间范围 | 行操作：提交交付确认（弹窗表单） |
| 我的交付记录 | 采购订单号、时间范围 | 查看历史交付 |
| 我的交付统计 | 采购订单号、是否超期、是否完成、时间范围 | 顶部统计卡片（/myStats）+ 下方明细表格（/myTimeliness） |
